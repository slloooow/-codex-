# Codex 额度桌宠验证报告

验证日期：2026-09-01（Asia/Shanghai）

## 本次兼容模式结果

- 已移除固定 `windowDurationMins = 300` 限制。
- `rateLimitsByLimitId` 存在时作为权威来源：优先检查 `codex` 主桶，在其 `primary` / `secondary` 中选总时长最短者；主桶没有有效窗口时才扫描其他桶。
- 相同时长固定按 `primary`、`secondary`、桶 ID 排序；选择不受剩余百分比、距离重置时间或重置先后影响。
- 多桶字段缺失时兼容旧版 `rateLimits` 单桶；两种字段同时存在时不合并。
- 动态窗口名已实现：10080 分钟为“周额度”，整天为“N天额度”，整小时为“N小时额度”，其他为“N分钟额度”。窗口名只进入鼠标提示和托盘提示，不进入大小气泡正文。
- 一天以内使用 `HH:MM:SS 后重置`；一天以上使用 `N天 HH:MM:SS 后重置`。
- 明确无限状态显示 `∞`、蕾米埃尔对白“今天的时间对我们格外慷慨。”和“当前没有额度限制”，使用额度充足动画且不触发重置刷新。
- 无限缓存断连时显示 `∞!`、原断连对白和“上次状态：无限 · 等待更新”。空多桶集合、`rateLimits: null` 或主/次窗口明确为 `null` 才判定无限；缺字段、格式损坏和登录错误不会误判。
- `account/rateLimits/updated` 只触发完整 `account/rateLimits/read`；已有读取请求未完成时会合并重复通知，不会直接用通知片段覆盖当前窗口。

## 构建与环境

- Windows 桌面：2560 × 1440。
- .NET Framework 4.8 Release：533325。
- 主程序：2,126,336 字节。
- 主程序 SHA-256：`29FC4BD3AA1722B7F2FF3BA1C2036EDAD15E402EC7478A00867CC60847A615A8`。
- 源码包：10,320,110 字节，SHA-256：`14033B6B06CC76810078562810C8F2EF5B3C591D8745DF830DAE12AB3C43C990`。
- 源码包包含 41 个文件，包括 6 个原始 360px GIF、6 个清理后 360px 母版、6 个 240px 运行时 GIF、边缘清理工具、解析/通知/对白/动作/交互测试和视觉探针；普通构建不需要 Python。
- 最终 ZIP 已解压到全新目录并独立完成构建，解析、通知、对白、交互和 12 个 GIF 资源测试再次通过。
- 交付目录与 `%LOCALAPPDATA%\CodexQuotaPet` 中的 EXE 哈希一致。

## 自动验证

- 额度解析：32 项断言通过。覆盖五小时加周额度、仅周额度、任意分钟窗口、主桶优先、主桶无有效窗口回退、全局最短、旧版单桶、相同时长、异常百分比、过期重置、明确无限、多桶权威性、字段缺失和格式损坏。
- 通知完整读取：5 项断言通过。验证通知片段不会更新快照、会发起一次完整读取、读取未完成时重复通知被合并、完整响应才更新快照、完成后下一次通知可再次读取。
- 漫画对白：43 项断言通过。覆盖所有额度边界、加载、有限缓存断连、无缓存断连、无限、无限缓存断连、过期刷新和周额度长倒计时。
- 动作状态映射：14 个场景通过，覆盖加载、陈旧、0% 优先级、额度分段及工作/哈欠/注意/开心动作。
- 窗口交互：25 项断言通过，覆盖后台更新保持收起、悬停展开、离开延迟、单击保持、拖动保持、鼠标穿透、刷新优先级、动态窗口提示、无限开心动画、无限不触发重置和陈旧托盘标记。
- GIF 母版：6 个文件通过；均为 360 × 360、17–61 帧、60–70ms、透明、无限循环。
- GIF 运行时资源：6 个文件通过；均为 240 × 240，逐帧时序、透明标记和循环与母版一致。
- GIF 白边：360px 母版近白外缘均为 0%，淡色外缘最高 0.11%，可见面积保留 98.14%–98.47%；240px 运行时资源近白和淡色外缘均为 0%。

## 真实 App Server 对照

- 原始多桶响应中，`rateLimitsByLimitId.codex.primary` 为 300 分钟，`secondary` 为 10080 分钟；新选择器按主桶内最短总时长选中 300 分钟窗口。
- 最终实时探针结果：`limitId=codex`、`windowDurationMins=300`、`windowName=5小时额度`、`usedPercent=66`、`remainingPercent=34`、`isUnlimited=False`、重置时间 `2026-09-01T06:58:01Z`。
- 这次真实响应仍同时存在五小时和周额度，因此当前显示五小时；若以后主桶仅剩周额度，解析测试已验证会自动切换为周额度。

## 视觉验证

- 使用正式 304 × 338 布局和 240px 角色资源生成 9 种状态：收起、五小时、周额度、15 分钟、无限、耗尽、加载、有限断连、无限断连。
- 9 种状态分别在棋盘格、深色和蓝色桌面背景离屏渲染；周额度长倒计时、无限对白、断连缓存、中文和粉色重点文字均无裁切。
- 实际安装后另以 2560 × 1440 桌面截图复查：桌宠位于右下角，透明区域正确，角色和小云朵边缘未见白底、淡紫光圈或透明键绿色残留，默认尺寸未大面积遮挡窗口。
- 六个 GIF 的动作分析图已重新生成；正式运行仍使用无缩放 240 × 240 动画，避免透明边缘插值。

## 安装与运行验证

- 已重新安装并启动 `%LOCALAPPDATA%\CodexQuotaPet\CodexQuotaPet.exe`。
- 已验证桌面快捷方式和当前用户开机自启项均指向安装目录。
- 第二次启动后 `CodexQuotaPet.exe` 进程数仍为 1，单实例有效。
- 桌宠子进程确认为隐藏的 `codex.exe app-server`。
- 60 秒轮询、本地每秒倒计时、有限窗口归零刷新、5/15/60/300 秒重连退避、拖动、穿透、双击链接和托盘菜单保持不变。
